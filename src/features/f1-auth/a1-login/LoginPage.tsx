import React from 'react';
import LoginContainer from './LoginContainer';

const LoginPage = () => {
    return (
        <div>
            <LoginContainer/>
        </div>
    );
};


export default LoginPage;